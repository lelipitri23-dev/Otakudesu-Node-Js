// public/sw.js
// Service worker minimalis agar PWA bisa di-install

self.addEventListener('fetch', (event) => {
  // Kosong saja sudah cukup untuk memenuhi syarat instalasi PWA
  // Anda bisa tambahkan logika caching di sini nanti
});